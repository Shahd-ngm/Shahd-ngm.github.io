
// code for the ghost 
document.addEventListener("DOMContentLoaded", function () {
    const ghost = document.querySelector(".ghost");
    ghost.style.animationPlayState = "running"; // Start the animation
});
